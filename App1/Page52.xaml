<Page
    x:Class="App1.Page52"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:App1"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">

    <Grid>
        <StackPanel 
            HorizontalAlignment="Center" 
            VerticalAlignment="Center" 
            Spacing="15"
            Width="400">

            <TextBlock 
                Text="Задание 52: Взаимодействие между элементами"
                FontSize="20"
                FontWeight="Bold"/>

            <TextBlock Text="Выберите категорию:" />
            <ComboBox 
                x:Name="CategoryComboBox"
                SelectionChanged="Category_SelectionChanged"
                PlaceholderText="Выберите категорию">
                <x:String>Категория 1</x:String>
                <x:String>Категория 2</x:String>
                <x:String>Категория 3</x:String>
            </ComboBox>

            <TextBlock Text="Элементы:" Margin="0,10,0,0"/>
            <ListView 
                x:Name="ItemsListView"
                Height="200"
                Width="350"/>

        </StackPanel>
    </Grid>
</Page>
