<Page
    x:Class="App1.Page98"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:App1"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">

    <Grid>
        <StackPanel 
            HorizontalAlignment="Center" 
            VerticalAlignment="Top" 
            Spacing="15"
            Width="400">

            <TextBlock 
                Text="Угадай число"
                FontSize="24"
                FontWeight="Bold"/>

            <TextBlock 
                Text="Введите число (от 1 до 100):"
                FontSize="16"/>
            <TextBox 
                x:Name="GuessTextBox" 
                PlaceholderText="Ваше число"
                Height="40"
                Width="350"/>

            <Button 
                Content="Проверить"
                Click="CheckGuess_Click"
                Width="150"/>

            <TextBlock 
                x:Name="HintTextBlock"
                Text=""
                FontSize="18"
                Foreground="DarkBlue"
                Margin="0,10,0,0"/>

            <TextBlock 
                Text="Попытки:"
                FontSize="16"
                FontWeight="Bold"/>
            <ProgressBar 
                x:Name="AttemptsProgressBar"
                Height="10"
                Width="350"/>

            <Button 
                Content="Новая игра"
                Click="NewGame_Click"
                Width="150"/>

        </StackPanel>
    </Grid>
</Page>