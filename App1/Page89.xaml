<Page
    x:Class="App1.Page89"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:App1"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">

    <Grid>
        <StackPanel 
            HorizontalAlignment="Center" 
            VerticalAlignment="Top" 
            Spacing="15"
            Width="400">

            <!-- 👇 Категории расходов -->
            <TextBlock Text="Категории расходов:" FontSize="18" FontWeight="Bold"/>
            <StackPanel x:Name="CategoriesPanel" Spacing="5"/>

            <!-- 👇 Сумма -->
            <TextBlock Text="Сумма:" FontSize="16"/>
            <TextBox 
                x:Name="AmountTextBox" 
                PlaceholderText="Введите сумму"
                Height="40"
                Width="350"/>

            <TextBlock Text="Категория:" FontSize="16"/>
            <ComboBox 
                x:Name="CategoryComboBox"
                SelectionChanged="Category_SelectionChanged"
                PlaceholderText="Выберите категорию">
                <x:String>Продукты</x:String>
                <x:String>Транспорт</x:String>
                <x:String>Развлечения</x:String>
                <x:String>Одежда</x:String>
            </ComboBox>

            <TextBlock Text="Процент от бюджета:" FontSize="16"/>
            <ProgressBar 
                x:Name="BudgetProgressBar"
                Height="10"
                Width="350"/>

            <TextBlock Text="Статистика:" FontSize="16" FontWeight="Bold" Margin="0,20,0,0"/>
            <TextBlock 
                x:Name="StatisticsTextBlock"
                Text=""
                FontSize="16"
                Foreground="DarkBlue"/>

            <Button 
                Content="Добавить расход"
                Click="AddExpense_Click"
                Width="150"/>

        </StackPanel>
    </Grid>
</Page>
