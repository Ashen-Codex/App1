<Page
    x:Class="App1.Page94"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:App1"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">

    <Grid>
        <StackPanel 
            HorizontalAlignment="Center" 
            VerticalAlignment="Top" 
            Spacing="15"
            Width="400">

            <TextBlock Text="Маршрут:" FontSize="16"/>
            <ComboBox 
                x:Name="RouteComboBox"
                SelectionChanged="Route_SelectionChanged"
                PlaceholderText="Выберите маршрут">
                <x:String>Москва → Санкт-Петербург</x:String>
                <x:String>Москва → Новосибирск</x:String>
                <x:String>Москва → Владивосток</x:String>
            </ComboBox>

            <TextBlock 
                x:Name="DistanceTextBlock"
                Text=""
                FontSize="16"
                Foreground="DarkBlue"/>

            <TextBlock Text="Карта:" FontSize="16" FontWeight="Bold"/>
            <Image 
                x:Name="MapImage"
                Width="350"
                Height="200"
                Stretch="UniformToFill"
                Visibility="Collapsed"/>

            <Button Content="Начать маршрут" Click="StartRoute_Click" Width="150"/>
            <Button Content="Сбросить маршрут" Click="ResetRoute_Click" Width="150"/>

            <TextBlock Text="Загрузка:" FontSize="16" FontWeight="Bold"/>
            <ProgressRing 
                x:Name="ProgressRing"
                IsActive="False"
                Width="50"
                Height="50"/>

        </StackPanel>
    </Grid>
</Page>
