<Page
    x:Class="App1.Page72"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">

    <Page.Resources>
        <ControlTemplate x:Key="CustomButtonTemplate" TargetType="Button">
            <Grid>
                <VisualStateManager.VisualStateGroups>
                    <VisualStateGroup x:Name="CommonStates">
                        <VisualState x:Name="Normal"/>
                        <VisualState x:Name="PointerOver">
                            <Storyboard>
                                <ColorAnimation
                                    Storyboard.TargetName="bgBrush"
                                    Storyboard.TargetProperty="Color"
                                    To="LightBlue"
                                    Duration="0:0:0.2"/>
                            </Storyboard>
                        </VisualState>
                    </VisualStateGroup>
                </VisualStateManager.VisualStateGroups>

                <!-- Используем Border вместо Rectangle -->
                <Border x:Name="bg" CornerRadius="5">
                    <Border.Background>
                        <!-- Именуем кисть, чтобы анимировать её Color -->
                        <SolidColorBrush x:Name="bgBrush" Color="Blue"/>
                    </Border.Background>
                </Border>

                <ContentPresenter 
                    Content="{TemplateBinding Content}"
                    HorizontalAlignment="Center"
                    VerticalAlignment="Center"/>
            </Grid>
        </ControlTemplate>
    </Page.Resources>

    <Grid>
        <Button 
            Content="Пользовательская кнопка"
            Template="{StaticResource CustomButtonTemplate}"
            Width="200" 
            Height="50"/>
    </Grid>
</Page>